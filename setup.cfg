[metadata]
license_file = LICENSE

[bdist_wheel]
universal = 1

[tool:pytest]
testpaths = tests

[coverage:run]
branch = True
source =
    src/ssm_config
    tests

[coverage:paths]
source =
    src/ssm_config
    .tox/*/lib/python*/site-packages/ssm_config
